## Archived — replaced by `bug-fix`

This skill was merged into `~/.claude/skills/bug-fix/` on 2026-05-21 as part of consolidating the two bug-handling skills into one.

- New trigger: `/bug-fix` (default mode runs the 6-phase diagnose loop: feedback-loop → reproduce → hypothesise → instrument → fix → cleanup)
- Original SKILL.md preserved as `SKILL.md.bak` for reference

Do not invoke this directory — its SKILL.md has been renamed so it is no longer registered as a skill.
